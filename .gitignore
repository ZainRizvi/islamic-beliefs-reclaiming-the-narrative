# OS / editor junk
.DS_Store
*.swp

# Large source assets kept out of version control
audio/
*.pdf

# Rendered book output + the processed render tree (regenerated from src/).
# The concept term text lives permanently in src/ @@CX(...)@@ markers, so the
# generated render tree under build/ is fully disposable.
book_en/build/

# Toolchain caches
book_en/.bundle/
book_en/vendor/bundle/
